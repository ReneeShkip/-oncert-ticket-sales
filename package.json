{
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dayjs": "^1.11.19",
    "multer": "^2.1.1",
    "nodemailer": "^8.0.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "build": "cd ticketstore && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/server.js"
  }
}
